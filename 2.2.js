    let min =Number(prompt("ENTRE UN PETIT NOMBRE"))
    let current =Number( prompt("UN NOMBRE NORMAL"))
    let max =Number(prompt("LE PLUS GRAND NOMBRE MNT"))

    if ((min<current) && (current < max)) {

        console.log("t'est trop chaud");

    }

    else {

        console.log("Les calculs sont pas bon Kevin");
    }